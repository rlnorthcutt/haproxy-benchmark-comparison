apiVersion: 1

# Configure Prometheus as the default data source for Grafana
# so the dashboard is ready to use on first launch.
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://benchmark-prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 5s
